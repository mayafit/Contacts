#!/bin/sh
# Pre-commit hook for code quality checks
# 1. Increments CalVer build version
# 2. Runs lint-staged for ESLint and related tests on staged files

# Increment version and stage version files
node scripts/increment-version.js
git add version.json package.json

# Run lint-staged
npx lint-staged
